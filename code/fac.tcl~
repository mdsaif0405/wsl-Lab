proc Factorial {x} {
   set i 1; set product 1
   while {$i <= $x} {
      set product [expr $product * $i]
      incr i
   }
   return $product
}
puts [Factorial 5]

set var {orange blue red green}
set var [lsort $var]
puts $var
